apiVersion: v1
kind: Service
metadata:
  name: my-api
spec:
  type: LoadBalancer # O ClusterIP si usas un Ingress
  selector:
    app: my-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
